# The whole repository requires review/approval from the owner.
# Combined with "Require review from Code Owners" on the main branch protection,
# this means only @salesrafa's approval can unblock a merge.
* @salesrafa
